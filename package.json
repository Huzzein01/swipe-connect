{
  "name": "swipe-connect",
  "version": "1.0.0",
  "description": "A mobile job-finding app with swipe interface and LinkedIn integration",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend-new && npx expo start\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend-new && npm install",
    "ios": "cd frontend-new && npx expo run:ios",
    "android": "cd frontend-new && npx expo run:android"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
